services:
  archipelago:
    build: .
    container_name: archipelago
    network_mode: host
    volumes:
      - ~/.ssh:/root/.ssh:ro
      - ./data:/data        # machines.json lives here — persists forever
    restart: unless-stopped
