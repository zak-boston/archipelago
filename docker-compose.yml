services:
  archipelago:
    build: .
    container_name: archipelago
    network_mode: host
    environment:
      - ARCHIPELAGO_PASSWORD=changeme   # ← set your password here
    volumes:
      - ~/.ssh:/root/.ssh:ro
      - ./data:/data
    restart: unless-stopped
